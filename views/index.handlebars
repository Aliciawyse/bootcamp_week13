<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Make a Burger and eat it!</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.6.1/css/bulma.min.css">
</head>
<body class="">

<section class="hero is-primary">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">Add Burger. Eat a burger.</h1>
        </div>
    </div>
</section>

<div class="tile is-ancestor">


    <div class="tile is-parent">

        <article class="tile is-child box ">

            <h2 class="has-text-weight-semibold is-size-4">Burgers that aren't devoured.</h2>
            <ul>
                {{#each burgers}}
                    <li>
                        <p>{{this.id}}. {{burger_name}}</p>
                    </li>
                {{/each}}
            </ul>

        </article>
    </div>
    <div class="tile is-parent">
        <article class="tile is-child box">

            <h2 class="has-text-weight-semibold is-size-4">Burgers that are devoured.</h2>
            <ul>
                {{#each burgers}}
                    {{#if devoured}}
                        {{> burgers/burgers-block devoured=false}}
                    {{/if}}
                {{/each}}
            </ul>
        </article>
    </div>

    <div class="tile is-parent">
        <article class="tile is-child box ">

            <form id ="create-form">

                <div class="form-group">
                    <label class="has-text-weight-semibold is-size-5" for="bg">Burger Name:</label>
                    <input class="textarea is-primary" type="text" placeholder="Burger name" type="text" id="burgerName" name="burger_name">
                </div>

                <button class="button is-primary button is-large " type="submit">Add Burger</button>
            </form>

        </article>
    </div>


</div>





</body>
</html>